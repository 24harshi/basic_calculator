class  extend{
    /**
     * The function calculates the square of a given number.
     * 
     * @param a The parameter "a" is a floating-point number that represents the side length of a
     * square.
     * @return the square of the input number 'a'.
     */
    float sqaure(float a){
    return (a*a);
}
    /**
     * The function cube takes a float value as input and returns the cube of that value.
     * 
     * @param a The parameter "a" represents the length of one side of a cube.
     * @return The cube of the input number 'a'.
     */
    float cube(float a){
    return (a*a*a);
}
    /**
     * The function calculates the modulus of two floating-point numbers.
     * 
     * @param a The first parameter "a" is a float value.
     * @param b The parameter "b" is the divisor in the modulus operation.
     * @return the modulus of the two input numbers, `a` and `b`.
     */
    float modulus(float a,float b){
    return(a%b);
}
}